<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Beamcast.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Beamcast"
>
    <Grid x:Name="RootGrid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid
                Grid.Column="0"
                Width="18"
                Height="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
            >
                <Ellipse Width="18" Height="18" Fill="{StaticResource BeamAccentSoftBrush}" />
                <Ellipse
                    Width="8"
                    Height="8"
                    Fill="{StaticResource BeamAccentBrush}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                />
            </Grid>
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="10"
                Margin="8,0,0,0"
                VerticalAlignment="Center"
            >
                <TextBlock Text="Beamcast" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                <Border
                    CornerRadius="6"
                    Padding="7,1"
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                >
                    <TextBlock
                        x:Uid="Title_StudyBadge"
                        FontSize="10"
                        FontWeight="SemiBold"
                        Opacity="0.8"
                    />
                </Border>
                <Border
                    x:Name="TitleLiveBadge"
                    Visibility="Collapsed"
                    CornerRadius="6"
                    Padding="7,1"
                    Background="{StaticResource BeamLiveBrush}"
                >
                    <TextBlock
                        Text="LIVE"
                        FontSize="10"
                        FontWeight="Bold"
                        Foreground="White"
                    />
                </Border>
            </StackPanel>
        </Grid>

        <InfoBar
            x:Name="UpdateBar"
            Grid.Row="1"
            Margin="60,0,20,4"
            IsOpen="False"
            Severity="Informational"
        >
            <InfoBar.ActionButton>
                <Button x:Uid="Update_BarAction" Click="OnUpdateBarAction" />
            </InfoBar.ActionButton>
        </InfoBar>

        <NavigationView
            x:Name="NavView"
            Grid.Row="2"
            PaneDisplayMode="LeftCompact"
            IsSettingsVisible="True"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsTitleBarAutoPaddingEnabled="False"
            OpenPaneLength="196"
            CompactPaneLength="48"
            Background="Transparent"
            SelectionChanged="OnNavSelectionChanged"
        >
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="LoungeNav" x:Uid="Nav_Lounge" Tag="lounge">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE716;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="AboutNav" x:Uid="Nav_About" Tag="about">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <Grid
            x:Name="FullscreenHost"
            Grid.Row="0"
            Grid.RowSpan="3"
            Visibility="Collapsed"
            Background="Black"
        />
    </Grid>
</Window>
